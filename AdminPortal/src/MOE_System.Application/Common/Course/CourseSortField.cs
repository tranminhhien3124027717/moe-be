namespace MOE_System.Application.Common.Course;

public enum CourseSortField
{
    CreatedAt = 0,
    CourseName = 1,
    Provider = 2,
    TotalFee = 3,
    StartDate = 4,
    EndDate = 5
}

public enum SortDirection
{
    Asc = 0,
    Desc = 1
}